target/
.env
data/
*.mv.db
*.trace.db
.idea/
*.iml
HELP.md

# Generated by the frontend build during `mvn package` (see pom.xml) —
# the frontend's own dist/ folder gets copied here on every build, so this
# should never be committed as source.
src/main/resources/static/
